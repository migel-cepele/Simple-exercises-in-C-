#include <iostream>
using namespace std;

int main() {

    int x, y;
    cout << "Vendosni 2 numra: ";
    cin >> x;
    cin >> y;
    int n, s;

    s = abs(x - 100);
    n - abs(y - 100);

    if (s > n) {
        cout << y;
    } else if (n > s) {
        cout << x;
    } else {
        cout << "0";
    }
    



}